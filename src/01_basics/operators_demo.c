#include <stdio.h>
int main(void){
    int a = 5, b = 2;
    printf("a + b = %d\n", a + b);
    printf("a / b (int) = %d\n", a / b);
    printf("a / b (double) = %.2f\n", (double)a / b);
    return 0;
}
